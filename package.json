{
  "name": "mobile-task-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Android wrapper for the mobile task tracker.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
